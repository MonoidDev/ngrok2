language: go
sudo: false

go: 
    - 1.9.x
    - 1.10.x
    - 1.11.x
    - tip

matrix:
    allow_failures:
        - go: tip

before_install:
    - go get -i github.com/jteeuwen/go-bindata/go-bindata

script:
    - make release-all
